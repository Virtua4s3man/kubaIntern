<form method="post" action="{{ path('wishlist_delete', {'id':product.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-success">Delete from wishlist</button>
</form>