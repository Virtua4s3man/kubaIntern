<form method="post" action="{{ path('wishlist_remove', {'id':product.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-success">{{ 'delete from wishlist' | trans }}</button>
</form>