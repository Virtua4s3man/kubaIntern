<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">{{ 'kubaIntern' | trans }}</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('product_index') }}">{{ 'main page' | trans }}<span class="sr-only">{{ '(current)' | trans }}</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'category' | trans }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% for category in categories %}
                        <a class="dropdown-item" href="{{ path('product_category_show', {id:category.id}) }}">{{ category.name }}</a>
                    {% endfor %}
                </div>
            </li>
        </ul>
    </div>
</nav>